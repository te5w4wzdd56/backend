package com.launcehub.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class BeanConfig {
    
    // Bean definitions moved to SecurityConfig to avoid duplication
}
